/** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactStrictMode: true,
//   swcMinify: true,
// }

// export default nextConfig
const nextConfig = {
  distDir: "build",
};

export default nextConfig